<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.jade.swp.persistence.UserMapper">

    <select id="login" resultType="User">
        select * from User where uid = #{uid} and upw = password(#{upw})
    </select>
    
    <update id="keepLogin">
        update User set sessionkey = #{sessionId}, sessionlimit = #{sessionLimit}
         where uid = #{uid}
    </update>
    
    <select id="checkLoginBefore" resultType="User">
        select * from User
         where sessionkey = #{loginCookie}
           and sessionlimit > now()
    </select>
    
</mapper>